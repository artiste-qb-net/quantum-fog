#pragma once#include "VECTOR.h"class STRINGY;//UI_MAP = unsigned integers map//******************************************class UI_MAP{public:	//newfo_map =new from old 	//oldfn_map =old from new	VECTOR<USHORT>	its_newfo;	VECTOR<USHORT>	its_oldfn;	UI_MAP();		VOID	set_to_default_map(USHORT	len);	UI_MAP(USHORT	len);	virtual ~UI_MAP();		VOID	set_newfo();	VOID	set_oldfn();	BOOLEAN	is_identity_map() const;	VOID	get_inverse(UI_MAP  &  inv);	VOID	 sort_vec_of_stringies_by_name(		const STRINGY   *   * 	stringies_p_p,		USHORT					len);	VOID	 sort_vec_of_longs_by_size(		const LONG   *  		items_p,		USHORT					len);};